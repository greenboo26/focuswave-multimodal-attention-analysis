# FocusWave 多模态分析算法库协作入口

进入本仓库后，先读取根目录 `AI_PROJECT.md`，再读取本文件、`README.md`、`docs/00_治理/`、当前项目状态与交接证据。`AI_PROJECT.md` 只负责项目身份和导航；分析结论、代码行为和结果真相以本仓库的规则、报告、测试、manifest 和 Git 证据为准。

中央跨 AI 治理唯一来源是 `greenboo26/ai-governance@main`；工作区项目注册表是 `greenboo26/project@august/PROJECT_INDEX.md`。本文件是本算法库的项目级入口，不替代中央治理，也不把相关的 NIR/RGB producer、实验程序或独立毫米波项目当成本仓库的科学真相。

## 进入与目录纪律

1. 先确认实际仓库路径、远程仓库、分支、当前提交和工作树状态；保留用户未提交内容，不 reset、clean、切分支或覆盖无关修改。
2. 先读现有项目管理和算法库规范，再决定新文件归属：`D:\Project\AGENTS.md`、`D:\Project\01_管理\01_项目管理\项目文件管理与AI执行规范.md`（存在于该工作区时）、`docs/00_治理/算法库目录与命名规范_v1.md`、`scripts/README.md`。
3. 不新造平行状态文档、平行 `output/` 结构、第二套脚本索引或临时 Git 仓库。优先复用既有的 `01_管理`、`docs/00_治理`、`scripts/`、`output/`、`CHANGELOG.md`、`PROJECT_STATUS.md`、`docs/handoff/` 和结果索引。
4. `work/` 只能承载临时运行物或待审计副本；已有 clone、Git worktree、运行态和历史快照不得用普通文件操作移动。清理前必须用 Git 的 worktree/分支/唯一提交证据确认没有活动任务或未交接成果。

## 实质进展的追溯门

任何新脚本、重跑、结果变化、方法决定、否决路线、输入语义修正、迁移或审计，只要产生实质进展，同一次工作必须更新能够承载该事实的既有记录：

- 项目管理层：`01_管理/项目状态.md`、`待办.md`、`分析记录.md`、`交接任务.md`；
- 追溯索引层：`图表索引.md`、`版本对应.md`、`资源索引.md`，以及本仓库的脚本索引、`CHANGELOG.md`、结果目录 README/manifest；
- 结果层：记录输入范围、生成脚本和版本、参数/QC、输出路径、证据状态、替代关系和下一步；大型输出、原始数据、视频、缓存、虚拟环境和运行态只保留本地，不进入 Git。

Git 提交说明至少要能回答：改了什么、为什么改、脚本/入口在哪里、结果/图表/表格在哪里、结论或决策是否变化、如何验证、下一步是什么。提交前只检查和暂存本次任务的明确路径，不把用户已有修改、原始数据、未登记输出或认证运行态带入提交。

不得把“文件存在”“本地提交”或“生成过输出”写成“已验证”“已推送”或“已完成全队列”。完成汇报必须给出实际路径、commit、验证证据和剩余风险，并使用 `PASS`、`PARTIAL` 或 `BLOCKED` 区分边界。
