# Tests

Lightweight schema, path, contract and staged-content checks; no broad scientific rerun.

行为科学 v3 契约测试只读取 [`fixtures/behavior_science_v3_synthetic.json`](fixtures/behavior_science_v3_synthetic.json) 合成 fixture，不读取真实数据，也不验证 v2 结果。
