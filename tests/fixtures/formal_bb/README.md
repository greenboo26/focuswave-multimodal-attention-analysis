# Formal BB synthetic fixture

`build_fixture.py` creates four synthetic BB sessions in three anonymous participant groups plus an explicitly excluded `sub-9504` pilot row. The generated data contain no real participant identifiers or measurements. Tests use the fixture to verify multi-scale outputs, repeated-session grouping, probe-window boundaries, signal-detection gates, and legacy-input rejection.
