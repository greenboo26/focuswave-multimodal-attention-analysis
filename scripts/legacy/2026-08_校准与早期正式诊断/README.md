# 历史校准与早期正式诊断

本目录保存 2026-08 的一次性诊断脚本、控制台日志和中间索引。它们用于追溯早期毫米波摆位、半频锁定和首批正式行为数据问题，不能作为当前正式分析入口。

- 原始数据路径写死在脚本中，当前可能未挂载。
- `_rest1_idx.npy` 仅服务 `_verify_rest1.py` 的历史复核。
- 现行 ECG/RSP 参照结果位于 `output/20_生理金标准验证/`；正式队列质量门控位于 `output/10_质量控制/` 与 `output/40_正式实验/`。
- 如需复用其中任一思路，应先迁移为新的命名规范脚本，并重新核验数据来源、时间轴和质量门控。


