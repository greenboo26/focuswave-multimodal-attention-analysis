# AMD/NVIDIA scientific contract candidate

Scientific equivalence is defined by schema, units, timestamp/gap policy, QC semantics and representative parity, not by branch name or bitwise equality. NVIDIA CPU-reference↔CUDA parity remains a separate gate; AMD DirectML RGB real-300 evidence is recorded as passed for the Py-Feat 2.1.1 scientific core, but this does not authorize RGB formal statistics.

## Gate 0 before bulk NIR

Use a frozen representative input/reference package, record all required provenance fields, and compare frame/timestamp identity, YOLO detection/ROI coverage, RITnet/fullclass outputs, missingness/QC and key continuous quantities under predeclared tolerances. A failed gate stops bulk AMD production. Do not compare different subjects across disks as row-wise parity. This candidate records `READY_TO_RUN`, not `PASS`, because the representative NIR cross-backend execution evidence is not present here.
