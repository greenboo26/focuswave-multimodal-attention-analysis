# NIR pipeline

External `Attention-Analysis` refs `nvidia-cuda` and `amd-DirectML` are producers. Local output must be standardized, timestamp/QC audited and packaged for central merge. PR #2 is diagnostic only.
