# RS6240 Sol 审阅问题清单

请基于本 PR 中的脚本、报告和 `work/rs6240_ecg_cross_session_v1/` 输出做独立方法学审阅。不要把未在仓库中的原始 ECG 或毫米波数据当作已核验事实，也不要把本轮 experimental 结果升级为正式结论。

## 需要裁决的问题

1. 在 4 个独立场次、80 个窗口、固定 `+0.365 s` 偏移下，T0 是否有足够证据替代 S1 成为正式 baseline？
2. T0 的 HR/IBI 误差改善是否被 beat recall、RMSSD 可用覆盖率和呼吸谐波误锁的不一致抵消？
3. 当前“场次级方向一致性 + 可用覆盖率”是否是比 pooled 平均差值更合适的报告依据？
4. 若不支持升级 T0，下一步最小而不引入新调参的验证是什么？

## 必须遵守的边界

- `sub-97793_` 是 evaluator 开发与冻结场次，不作为跨场次泛化证据。
- `sub-97794_` 与实际目录 `sub-97994_` 是既有数据别名，不是两个被试。
- `S1` 保持 formal baseline；`T0` 保持 experimental；`C8-PH` 不进入本轮裁决；C8 complex coherent fusion 继续封锁。
- 不重新估计时间偏移，不按场次或模型调参，不将 ECG 参考标签用于毫米波目标选择。

## 建议输出格式

请给出：结论（支持/不支持/证据不足）、逐指标依据、跨场次重复性判断、最小补充验证，以及可直接写入报告的谨慎表述。每项都应引用仓库中的具体文件路径。
