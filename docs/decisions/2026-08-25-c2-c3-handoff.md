# C2/C3 handoff：C1b blocker 后的当前结果

日期：2026-08-25
决策依据：`76573b0cdddd27c414b39ca71e565bf0377a65d6`

## 状态总览

- `C1a = DONE`
- `C1b = BLOCKED`：正式 VS_DATASET healthy cohort 与同步 Mindray reference 尚未到位；不再调 VitalSense 12 个示例。
- `C2 = COMPLETE / awaiting GPT-user adjudication`
- `C3 = COMPLETE / SESSION_ALIGNED_ONLY / PARTICIPANT_IDENTITY_BLOCKED`

本记录只共享关键状态、结果和开放问题。完整预测表、NIR probe 表和派生数据留在本地项目数据目录，不提交到 GitHub。

## C2 radar-only attention baseline v1

### RUN_ID 与复现信息

- `RUN_ID`: `C2_RADAR_ONLY_ATTENTION_BASELINE_V1_20260825`
- 执行时仓库 HEAD：`5da545e1789fc16a0841786dff20846f98f5dcad`
- 输入：既有 71-session / 1,317-probe M1/Q0 矩阵；未重新扫描原始 NPZ。
- 标签：既有 thought-probe 标签，`label=1`（专注）对 `label=2/3/4`（非专注）；未改称 workload、fatigue 或 arousal。
- 分组：leave-one-`repeat_participant_id`-out，46 个已恢复重复参与者、71 个 recording sessions；预处理只在训练折拟合。
- 模型：固定 L2 logistic regression、参与者级训练权重、1,000 次参与者 bootstrap。

### 关键结果

| 特征组 | ROC-AUC | 平衡准确率 | 相对 B0 AUC |
|---|---:|---:|---:|
| N0 grouped null | 0.628 | 0.594 | -0.028 |
| B0 behavior baseline | 0.656 | 0.612 | 0 |
| R respiration | 0.622 | 0.586 | -0.034 |
| C cardiac candidate（探索性） | 0.626 | 0.594 | -0.030 |
| M raw micromotion/phase | 0.609 | 0.584 | -0.047 |
| Q quality descriptors | 0.611 | 0.571 | -0.045 |
| RADAR_ALL | 0.593 | 0.564 | -0.064 |
| B0 + RADAR_ALL | 0.619 | 0.581 | -0.037 |

当前运行结果不支持“现有雷达特征在该矩阵上对行为 baseline 有增量预测”的表述。C2 也没有证明毫米波无效，只说明在本轮既有特征、标签和分组下未观察到增量优势。`C_cardiac_candidate_exploratory` 不是已验证的心脏生理指标。

质量门控方面，1,317/1,317 行均满足已有 `q_extraction_ok == 1`，coverage 为 100%；因此本轮不能估计质量门控带来的性能变化，也没有新增阈值或选择性排除。

完整结果位于本地：

`D:\Project\厚粲杯\11_数据\derived\c2_radar_only_attention_baseline_v1\`

## C3 NIR QC/integration

### RUN_ID 与复现信息

- `RUN_ID`: `C3-NIR-QC-INTEGRATION-V1-20260825`
- 决策依据：`76573b0`
- 复用既有 NIR v2 probe 表与质量层级表；未重跑 NIR、未读取原始视频、未调 QC 参数。
- 时间窗口：`[probe_onset_unix_ms-60000, probe_onset_unix_ms)`。
- 时间语义：绝对 Unix 毫秒。

### 关键结果

- probe-aligned rows：340。
- unique probe keys：340；duplicate probe keys：0。
- `participant_id`：340 条全部留空，不能声称 participant-level LOSO。
- 质量层级：`primary_ge80 = 265`，`sensitivity_50_to_lt80 = 10`，`exclude_lt50 = 65`。
- 失败语义保留为 `partial_probe_coverage` 或 `no_usable_frame_in_any_probe_window`，没有自行填补。
- 当前 readiness：`SESSION_ALIGNED_ONLY / PARTICIPANT_IDENTITY_BLOCKED`。
- `pupil_equiv_diameter` 保留为标准化 ROI 像素，不是毫米，也不是 attention ground truth。

完整 NIR 产物留在本地：

`D:\Project\厚粲杯\11_数据\derived\c3_nir_qc_integration_v1\`

## Open questions for GPT/user

1. 是否确认 C2 的既有 `label=1` 对 `label=2/3/4` 继续作为 primary endpoint。
2. 后续若要启用 quality gating，需要先冻结阈值、排除规则和成功标准，不能从本轮结果反推。
3. C2 是否进入下一阶段的特征/模型改进，必须先决定改进目标是提高 radar-only 泛化、提高 coverage，还是转向 NIR-assisted teacher 路线。
4. C3 在真实 participant identity 恢复前，不进入 participant-level LOSO 或正式 NIR–mmWave 融合性能结论。

## 禁止的当前表述

- 不得写“毫米波 HRV 已验证”。
- 不得写“毫米波已经不能预测专注”。
- 不得把 C2 的 cardiac candidate 特征称为已验证生理指标。
- 不得把 C3 的 session-aligned 表称为 participant-level 数据集。
- 不得改变 C1b 的冻结时延、匹配、split 或评价协议。
