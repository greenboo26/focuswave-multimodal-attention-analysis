# 项目级进度与时空状态地图

状态：`CURRENT_PROJECT_STATE_MAP`

更新时间：2026-08-29（Asia/Shanghai）

本文件是本仓库当前项目状态的治理地图。它用于把阶段、历史工作、冻结结论和下一决策放在同一处；不替代分析历史账本、结果报告或具体证据文件。

## 当前阶段

- 项目阶段：毫米波正式资格收口、证据协调与报告整合；不是开放式算法重研。
- 分析阶段：B2 已完成；当前不授权因 B2 自动重跑 HR、BR 或 HRV。
- 仓库身份：`greenboo26/focuswave-multimodal-attention-analysis`，canonical 分支 `main`。
- 当前输入范围：B2 仅涉及既有 formal corrected-distance 表、既有 DataCube 和历史 target；不包含 NIR/RGB。

## 已完成

- B2 完成 16 个 corrected distance `<0.30 m`、18 个 `>1.50 m` 和 9 个 `0.30–0.60 m` reference 的只读前端比较，共 43 个 session。
- 43 个 DataCube 均存在并生成 43 张诊断图；距离统一按 `distance_m = bin × 0.037`。
- 保留既有 heart/breath target，不重新选择 target；不改变 gate、不重跑 HR/BR、不训练分类器、不做 HRV、不读取 NIR/RGB。
- 明确 `expected human range unavailable`；heart 与 breath 独立分类。
- corrected 37 mm formal QC 重算：Tier 1=33、Tier 2=37、Tier 3=2（067、099）。
- BIOPAC corrected calibration：HR-course MAE=3.777 bpm；BR spectral MAE=3.328 breaths/min。旧 4.59/4.61 与 3.51 只保留 historical old-gate 口径。

## 当前冻结结论

- B2 总体结论：`RISK_NOT_SUPPORTED`。
- 当前没有成批 session-level target 证据支持 `LIKELY_NEAR_FIELD_OR_DIRECT_LEAKAGE` 或 `LIKELY_FIXED_ENVIRONMENT_REFLECTION`。
- 共同近距离亮带是可见的前端结构，但在缺少摆位 ground truth 时不能定性为人体、直接泄漏或固定环境反射。
- `LIKELY_HUMAN` 仅是少数 target 的保守形态标签，不是人体摆位真值，也不代表 HR/BR/HRV 已验证。
- generic multi-bin / AoA / beamforming / VMD-grid 不得在无新证据时重新包装为下一步。
- 8 通道逐通道候选搜索/一致性与真正相干 AoA/beamforming 必须区分；后者仍为 `UNVERIFIED`。
- Behavior+mmWave C2B/C2C 已完成为 ablation，不重跑。
- HRV 在当前比赛周期保持 blocked/frozen，不因局部修正自动重开。

## 活跃事项

- #15：HR/BR 正式质量资格收口。
- #16：只补预先定义的一次 quality-stratified sensitivity。
- #17：统一最终矩阵、分母与 provenance。
- 将中央治理 v0.1.8 的 Project State / Reuse / Stage Gate 作为新任务入口。

## 阻塞/限制

- B2 缺少 session-level 雷达—人体摆位 ground truth，因此不能由异常距离直接推出物理原因。
- 33 个 Tier 1 仍只是 QC-eligible candidates，不是 33 场 ground-truth HR/BR pass。
- 角度/相干波束形成缺少正式 1D 输出链中的相位校准、TDM 时序和已知角度金标准闭环。
- NIR/RGB 仍按各自 producer/项目阶段管理，不因本地图自动进入毫米波主结论。

## 已被替代 / 历史边界

- `0.08 m/bin` 距离语义已被正式 `0.037 m/bin` 替代。
- HR-course 4.59/4.61 bpm 只作 historical old-gate；当前 corrected MAE=3.777 bpm。
- BR 3.51 breaths/min 只作 historical old-gate；当前 corrected spectral MAE=3.328 breaths/min。
- “异常距离自动意味着近场泄漏或固定环境反射”已被 B2 `RISK_NOT_SUPPORTED` 替代。
- “局部 bug、单一指标或新细节出现即可重开整条路线”不再允许；重开必须有新证据、失败验收门、材料身份/范围/新鲜度变化或明确用户要求。

## 下一决策

1. 以本地图、`ANALYSIS_HISTORY_LEDGER.md` 和 canonical progress map 为入口继续 #15 收口。
2. #15 达到退出条件后，只执行 #16 已预定义的 quality-stratified sensitivity。
3. #17 统一矩阵与报告口径；不把局部算法修复扩展成全链重跑。
4. 若要重开 B2、AoA/beamforming、旧七类方法或 HRV，必须先记录旧结论、使其失效的新证据、范围、停止条件和验收标准。

## 证据定位

- `ANALYSIS_HISTORY_LEDGER.md`
- `docs/canonical/ANALYSIS_PROGRESS_MAP_2026-08-28.md`
- `docs/canonical/MMWAVE_CURRENT_STATE_2026-08-29.md`
- B2 原始审计：`FORMAL_EXTREME_RANGE_TARGET_AUDIT.md`（本地 Codex 2026-08-29 交付）
