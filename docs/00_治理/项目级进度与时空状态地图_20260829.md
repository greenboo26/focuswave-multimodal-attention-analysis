# 项目级进度与时空状态地图

状态：`CURRENT_PROJECT_STATE_MAP`

更新时间：2026-08-29（Asia/Shanghai）

本文件是本仓库当前项目状态的唯一新增治理地图。它用于把阶段、历史工作、冻结结论和下一决策放在同一处；不替代分析历史账本、结果报告或具体证据文件。

## 当前阶段

- 项目阶段：跨仓库治理修复；毫米波前端 target/range 证据边界已完成 B2 收口，等待下一阶段决策。
- 分析阶段：B2 已完成；当前不授权因 B2 继续重跑 HR、BR 或 HRV。
- 仓库身份：`greenboo26/focuswave-multimodal-attention-analysis`，canonical 分支 `main`。
- 当前输入范围：B2 仅涉及既有 formal corrected-distance 表、既有 DataCube 和历史 target；不包含 NIR/RGB。

## 已完成

- B2 完成 16 个 corrected distance `<0.30 m`、18 个 `>1.50 m` 和 9 个 `0.30–0.60 m` reference 的只读前端比较，共 43 个 session。
- 43 个 DataCube 均存在并生成 43 张诊断图；距离统一按 `distance_m = bin × 0.037`。
- 保留既有 heart/breath target，不重新选择 target；不改变 gate、不重跑 HR/BR、不训练分类器、不做 HRV、不读取 NIR/RGB。
- 明确 `expected human range unavailable`；heart 与 breath 独立分类。

## 当前冻结结论

- B2 总体结论：`RISK_NOT_SUPPORTED`。
- 当前没有成批 session-level target 证据支持 `LIKELY_NEAR_FIELD_OR_DIRECT_LEAKAGE` 或 `LIKELY_FIXED_ENVIRONMENT_REFLECTION`。
- 共同近距离亮带是可见的前端结构，但在缺少摆位 ground truth 时不能定性为人体、直接泄漏或固定环境反射。
- `LIKELY_HUMAN` 仅是少数 target 的保守形态标签，不是人体摆位真值，也不代表 HR/BR/HRV 已验证。

## 活跃事项

- 将中央治理 v0.1.8 的状态优先、Reuse Gate（复用门）、Stage Gate（阶段门）和状态地图要求纳入本仓库执行入口。
- 等待主控对 B2 边界和下一阶段是否开展预先定义的最小 HR/BR 验证作出决策。

## 阻塞/限制

- B2 缺少 session-level 雷达—人体摆位 ground truth，因此不能由异常距离直接推出物理原因。
- 毫米波 HR、BR、HRV 仍不得写成已验证生理结论；任何新路线必须先定义效度证据、停止条件和验收标准。
- NIR/RGB 仍按各自 producer/项目阶段管理，不因本地图而自动进入毫米波主结论。

## 已被替代的工作假设

- “异常距离自动意味着近场泄漏或固定环境反射”已被 B2 `RISK_NOT_SUPPORTED` 替代。
- “局部 bug、单一指标或新细节出现即可重开整条路线”已被项目状态优先规则替代；重开必须有新证据、失败验收门、材料身份/范围/新鲜度变化或明确用户要求。

## 下一决策

1. 接受或驳回 B2 `RISK_NOT_SUPPORTED` 作为毫米波前端证据边界。
2. 若接受，决定是否进入预先定义的最小 HR/BR 验证；不自动进入 HRV。
3. 若重开 B2，先记录重开理由、使旧结论失效的新证据、范围、停止条件和验收证据，并保留原结论历史。

## 证据定位

- B2 报告：`C:\Users\550ACW\Documents\Codex\2026-08-29\c2-br-pipeline-br-datacube-target\outputs\FORMAL_EXTREME_RANGE_TARGET_AUDIT.md`
- B2 分类表：同目录 `FORMAL_EXTREME_RANGE_TARGET_CLASSIFICATION.csv`
- B2 reference：同目录 `FORMAL_EXTREME_RANGE_REFERENCE_SAMPLE.csv`
- B2 诊断图目录：同目录 `extreme_range_target_figures\`
- 本仓库历史路线索引：`ANALYSIS_HISTORY_LEDGER.md`
