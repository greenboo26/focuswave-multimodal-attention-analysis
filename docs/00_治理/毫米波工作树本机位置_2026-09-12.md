# 毫米波工作树本机位置（2026-09-12）

## 用途与边界

本文登记 `greenboo26/focuswave-multimodal-attention-analysis` 的两个仍含未提交工作的毫米波 Git 工作树。它们是同一仓库的关联工作树，不是可随意合并到 `08_算法` 主工作树的普通目录。依据算法库目录纪律，`08_算法/work/` 只放临时运行物或待审计副本；持续使用的 Git 工作树统一登记在同级 `08_算法_worktrees/`。主工作树保留其既有未提交内容；每项毫米波修改应继续在所属工作树中检查、测试和提交。

原 `C:\Users\550ACW\Documents\ChatGPT\毫米波\` 下的两份目录仅为已核验迁移后的待清理副本，不再作为 Git 关联工作树使用。

## 当前位置与状态

| 工作内容 | 本机位置 | HEAD | 当前未提交范围 |
|---|---|---|---|
| DLL 时间戳最小修复 | `D:\Project\厚粲杯\08_算法_worktrees\mmwave_dll_timestamp_minimal_b2fddca` | `b2fddca2542859f62d5f4b57f1b4fdecd54a5b4c` | frozen-cohort/probe-merge 脚本与 Issue 33、DLL 时间戳测试 |
| DLL 主机时间切换 | `D:\Project\厚粲杯\08_算法_worktrees\mmwave_dll_host_time_cutover` | `9cbaca004aa1f6e4977b620e4efda247f343f932` | 帧时间合同、毫米波验证/merge-ready 脚本、v3.1.1 和 DLL 时间戳测试 |

两份工作树均由 `D:\Project\厚粲杯\08_算法\.git` 统一登记，远程均为 `https://github.com/greenboo26/focuswave-multimodal-attention-analysis.git`。

## 迁移核验

- 每份工作树已逐文件计算 SHA-256，与迁移前副本一致：前者 646 个非 Git 内部文件，后者 643 个。
- 已执行 Git 工作树修复和对象库完整性检查；工作树状态与迁移前一致。
- 本次迁移只调整本机工作树位置，不改变原始数据、`11_数据` 派生数据、正式分析结果或科学结论。

## 使用规则

1. 先进入对应工作树，再运行该工作树所需的检查或测试；不要从 `08_算法` 主工作树复制覆盖这些目录。
2. 提交时只暂存本次确认的文件，保留既有未提交修改的来源与范围。
3. 原目录副本确认不再被进程或人工使用后再删除；不得把其存在误写为第三个有效工作树。
