# 脚本索引（scripts/）

> 命名规范（2026-08-14 起）：新脚本一律 `{功能}_{对象}_{MMDD}.py`；旧脚本保持原名；废弃脚本移 `scripts/archive_历史版本/`。
> 本索引分「活跃脚本（根目录）」与「归档脚本（archive_历史版本/）」两部分。

## 活跃脚本（根目录，27 个）

### 主线管线（当前分析入口）

| 脚本 | 用途 |
|------|------|
| `analyze_mmwave_hrv.py` | HR/HRV 提取主线（v1.3：窗级自适应选 bin + VMD + 多 bin 交叉验证 + 段参考修正） |
| `analyze_mmwave_full.py` | 全程窗特征 + 探针特征 + 行为对应（主线输出） |
| `assess_preexp_quality.py` | 心跳质量独立评估（SNR/IBI 窗级门控） |
| `motion_gate.py` | 摄像头 1Hz 运动量 × 毫米波质量门融合（v1.6 核心工具） |
| `analyze_rest_3min.py` | 3 分钟静止 HR/BR/HRV 诊断（被主线 import，勿移动） |

### 事件相关与跨被试

| 脚本 | 用途 |
|------|------|
| `analyze_erp_errors.py` / `analyze_erp_nogo.py` | 行为错误事件相关分析 |
| `analyze_probe_effect.py` / `analyze_survey_physio.py` | 探针效应 / 问卷-生理对照 |
| `analyze_rest_hrv_changes.py` / `analyze_rest_recovery_behavior.py` | 休息 HRV 变化 / 休息恢复 |
| `analyze_preexp_robustness.py` / `compare_preexp_hrv.py` / `diagnose_poor_windows.py` | 稳健性检验 / 跨被试对比 / 差窗诊断 |

### 外部数据集验证（2026-08-14）

| 脚本 | 用途 |
|------|------|
| `validate_external_gold_0814.py` | 金标准验证管线（多 bin 投票 + HPS + 轨迹 + quality 门控），220 会话 MAE 中位 9.3 |
| `analyze_external_heartbeat_0814.py` | TI 原始 ADC 跨设备跑通 |

### 绘图与报告

| 脚本 | 用途 |
|------|------|
| `plot_vitalsign_pipeline_0813.py` | 生命体征 8 步流程图（3D mesh / 全流程 / 批量） |
| `gen_range_time_maps.py` | 全程距离-时间热图 |
| `gen_preexp_reports.py` / `export_window_matrix.py` | 标准化报告 / 特征矩阵导出 |

### 管线基础模块（被 import，勿删除勿改名）

`process_vital_signs_v2.py`（常量+带通）、`process_vital_signs_v3.py`（VMD 心跳）、`process_vital_signs_v5.py`（呼吸峰值）、`process_vital_signs_v9.py`（谐波陷波）、`hrv_nonlinear.py`（SampEn/DFA）

### 数据工具

`truncate_preexp_data.py`（截断）、`rename_preexp_subject.py`（改编号）、`tools/check_preexp_data.py`、`tools/compare_all_datasets.py`

## 归档脚本（archive_历史版本/，可追溯不复用）

| 分组 | 脚本 | 归档原因 |
|------|------|----------|
| 31 项 A/B 实验 | `experiment_{spc,hampel,phasediff,cfar,ssa,envelope,ceemdan}.py`、`experiment_video_motion.py`、`experiment_video_roi.py`、`experiment_erp_fine.py` | 一次性实验，结论已入优化决策记录 |
| VMD 参数实验 | `experiment_vmd_params_0814.py` | 自适应无收益（实验 36） |
| 旧批次分析 | `analyze_deep_breath.py`、`compare_4subjects.py`、`analyze_angle.py`、`validate_rest_baseline.py`、`plot_rest_hrv_per_subject.py` | 旧批次（8/1 采集），不再主动更新 |
| 近场测试系列 | `analyze_nearfield_test.py`、`analyze_preexp_nearfield.py`、`assess_nearfield_mainlobe.py`、`compare_preexp_nearfield.py` | 近场方案测试完成 |
| 数据整理（已完成） | `merge_preexp_surveys_0813.py`、`analyze_preexp_behavior_0813.py` | 一次性合并/汇总，产出已入报告 |
| 其他 | `pack_expert_results.py` | 专家评审打包完成 |
| 更早历史 | `process_vital_signs_v1/v2_0-v2_4/v4/v6/v7/v8.py` 等 | 旧管线版本，git tag 可回溯 |
