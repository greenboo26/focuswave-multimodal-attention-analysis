# J:\Data NIR 教师信号第一阶段审计报告

## 结论

第一阶段线性混合效应模型目前阻塞，未运行 HR/ΔHR/BR 与 `pupil_mean`、`pupil_slope`、`blink_rate` 的模型。原因是当前 NIR 输出没有这三项目标特征，且现有特征来自探针前 60 秒旧窗口，没有当前毫米波事件锁定分析要求的事件后 30 秒恢复窗。

NIR（near-infrared，近红外）在本路线中仅作为视觉生理参考/教师信号，不能作为注意状态真值。未训练分类器，SDNN/RMSSD 未作正式生理推断。

## 资产与可复用输出

- `J:\Data` 共 72 个 NIR 目录，主毫米波事件批次 70 名被试；主批次 AVI 和 NIR 时间戳均为 70/70 完整，时间戳单调性为 70/70。
- 未发现名为 `nir_probe_features` 的现成文件。
- 可复用旧版矩阵：`D:\Project\厚粲杯\08_算法\output\J_Data_MULTIMODAL_v1\multimodal_probe_matrix.csv`，1,297 行、70 名被试。
- 现有 NIR 代理字段：`nir_pupil_dark_fraction`、`nir_eye_contrast`、`nir_pupil_detected`、`nir_pupil_radius_px`、`nir_quality`。这些是固定 ROI 暗核心、眼部对比度和 Hough 圆几何代理，不等同于标定瞳孔均值、斜率或眨眼率。

## 对齐审计

- 主键按 `subject + probe_id` 连接。现有多模态矩阵覆盖 1,297 个事件；与当前 1,400 个事件表重叠的 1,297 行中，`probe_onset_ms` 完全一致（1,297/1,297）。
- 原始 NIR 时间戳到事件零点最近帧偏差：中位数 7 ms，最大 42 ms，1,400/1,400 个事件不超过 100 ms。
- 旧版视觉代理匹配 1,158 行；其历史时间偏差中位数 0.082 秒，最大 0.138 秒。该指标不能替代同一事件窗的 NIR 特征审计。

## 缺失项与后续模型门控

缺失 `pupil_mean`、`pupil_slope`、`blink_rate`、逐帧瞳孔/眼睑轨迹、事件后 30 秒 NIR 窗和冻结的质量门控。只有在同一 J:\Data 事件窗重新提取并验证这些字段后，才执行以被试随机截距的 HR、ΔHR、BR 线性混合效应模型，控制 Block、探针顺序和 NIR 质量。SDNN/RMSSD 不进入正式模型。

## 文件与验证

审计脚本：[audit_J_Data_NIR_teacher_stage1.py](D:\Project\01_管理\audit_J_Data_NIR_teacher_stage1.py)。结果目录：`D:\Project\厚粲杯\08_算法\output\J_Data_NIR_TEACHER_STAGE1`，含资产清单、逐探针对齐表、字段字典、JSON 审计和详细 Markdown 报告。脚本已通过 `python -m py_compile`，并完整运行；输出计数、键连接、时间偏差和阻塞状态已从 JSON 与 CSV 复核。

相关仓库只读核对：FocusWave `6f6dd0f`（工作树干净）；`attention-pipeline-v2` HEAD `825238c`（工作树未修改）。
