# D1 北京—珠海 Canonical Harmonization：脱敏运行摘要

状态：`IDENTITY_OR_SESSION_LINKAGE_BLOCKED`

已实际运行的入口复用了北京冻结的 deterministic identity join 与 probe 行为资产，并将珠海 30 条登记记录保留在本地 canonical crosswalk。当前可读输入中珠海实际行为、`master_timeline`、probe response/vigilance、绝对时间、B1/B2/B3 边界和 session 级模态映射均为 0 条确定性链接。因此，本次没有产生或声称跨站点效应量、95% CI、显著性、敏感性结论或报告级跨站点图。

这不改变已冻结的协议事实：北京与珠海正式 B1/B2 属于同一 measurement protocol family。阻断的是珠海实际 session 层数据接入，不是协议裁决。完整 pseudonymous crosswalk 和行级 probe master 只存于 `D:\Project\厚粲杯\11_数据\derived\beijing_zhuhai_canonical_harmonization_v1`，未提交 Git。

重跑要求：为每个珠海实际 session 提供或确定性关联行为 CSV、`master_timeline.csv`、probe 标识/response/vigilance、absolute Unix ms、block 边界和模态目录，再运行同一入口。届时按照 `repeat_participant_id` 聚类，同时报告全部有效正式 session 与每人最早三场的预定义敏感性分析。
