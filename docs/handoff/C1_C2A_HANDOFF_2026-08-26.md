# C1 封存与 C2a 启动交接

日期：2026-08-26

## C1

C1c/C1d 已完成本地复核。最终状态为：

`C1D_NO_MATERIAL_IMPROVEMENT_STOP_HRV`

本状态仅表示当前比赛周期停止继续投入逐搏 IBI/HRV 算法开发，不表示 RS6240 在一般意义上无法测量 HRV。

C1c 重放与原 primary 结果一致后，C1d 仅在固定 waveform 上运行 Radar-Beat-style template/similarity/global-DP 后端。三场均未改善，平均 F1 下降 0.0509，IBI MAE 均恶化。因此不扩展到 48 session，也不再追加 C1 前端或后端调参。

## C2a

C2a 只做正式数据审计，最终状态应为 `C2A_DATASET_AUDIT_COMPLETE` 或列出具体 blocker。审计必须直接基于 J 盘正式数据和实际行为/问卷/时间对齐文件，不根据旧报告猜字段，不训练模型，不做大规模视觉特征提取，不重做 HRV。

必须交付：

- `C2A_DATASET_AUDIT.md`
- `c2a_sample_manifest.csv`
- `c2a_subject_group_map.csv`
- `c2a_label_summary.csv`
- `c2a_modality_coverage.csv`
- `c2a_manifest.json`

行级 manifest 和 subject mapping 只留在本地 derived；GitHub 只收脱敏汇总、schema、方法报告和审计脚本。

